# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
httpx>=0.27.0
python-multipart>=0.0.18   # FastAPI Form/UploadFile

# LangGraph + LangChain (agentes IA)
langgraph>=0.2.50
langchain>=0.3.10
langchain-openai>=0.2.14
langchain-core>=0.3.20
langchain-community>=0.3.10
openai>=1.58.1,<2.0.0

# Vector store (RAG de cursos) — aiohttp<3.10 requerido por langchain-pinecone
pinecone-client>=5.0.0
langchain-pinecone>=0.2.0
aiohttp>=3.9.5,<3.10

# Memory
redis>=5.2.0
asyncpg>=0.29.0            # Postgres (Supabase)

# Logging / rate limit / observability
structlog>=24.4.0
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.18.0

# Cloudflare R2 (S3-compatible) para media del inbox
boto3>=1.35.0

# Scheduler persistente (retargeting, monitoreo de pagos)
APScheduler>=3.10.4

# Prometheus metrics — endpoint /metrics con histogramas de latencia,
# contadores de requests, size de responses. Scrapeable por Prometheus
# self-hosted en el mismo droplet (ver docker-compose.yml).
prometheus-fastapi-instrumentator>=7.0.0

# Alembic — migraciones versionadas de Postgres con rollback. Reemplaza
# los scripts SQL manuales que vivían en migrations/ sin tracking.
alembic>=1.13.0
sqlalchemy>=2.0.0   # Alembic lo necesita aunque usemos asyncpg puro en la app
