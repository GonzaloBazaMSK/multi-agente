# Dev/test deps. NO se instalan en prod (Dockerfile usa requirements.txt solo).
# Uso local:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest

-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
